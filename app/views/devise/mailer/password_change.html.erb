<%= render partial: 'devise/mailer/shared/header' %>

<%= render partial: 'devise/mailer/shared/body', locals: {
  subject: t('devise.mailer.password_change.change_my_password'),
  icon: mail_image('Lock.png'),
  welcome: t(
    'devise.mailer.password_change.hello_user', 
    user: @resource.email
  ),
  paragraphs_before_link: [
    t('devise.mailer.password_change.we_re_contacting_you_to_notify_you_that_your_password_has_been_changed')
  ]
} %>

<%= render partial: 'devise/mailer/shared/footer' %>