services:
  web: 
    build: .
    command: bash -l -c "rm -f /encore/tmp/pids/server.pid && bundle exec rails s -p 3000 -b '0.0.0.0'"
    depends_on: 
      - db
      - redis
      - webpacker
    ports: 
      - "3000:3000"
    volumes:
      - .:/encore
      - enc_bundler_gems:/usr/local/bundle/
    env_file: '.env'

  db:
    image: postgres:9.4
    #image: postgres
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      timeout: 45s
      interval: 10s
      retries: 10
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./entrypoints/init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432"
    env_file:
       - '.env'

  redis:
    image: redis
    ports:
      - "6379"
    env_file:
      - '.env'
    volumes:
      - enc_redis_data:/data
 
  sidekiq:
    build:
      context: .
    volumes:
      - .:/encore
    command: /bin/bash -l -c "bundle exec sidekiq -C config/sidekiq.yml"
    depends_on:
      - db
      - redis
    env_file:
      - '.env'

  webpacker:
    build: .
    volumes:
      - .:/encore
      - enc_bundler_gems:/usr/local/bundle
   #command: /bin/bash -l -c "rm -rf /encore/app/public/packs; rm -rf /encore/tmp/cache/webpacker; /encore/bin/webpack-dev-server"
    command: /bin/bash -l -c "rm -rf /encore/app/public/packs; rm -rf /encore/tmp/cache/webpacker; yarn dev"
    ports:
      - '3035:3035'
  
volumes:
  db_data:
  enc_redis_data: 
  enc_bundler_gems:
